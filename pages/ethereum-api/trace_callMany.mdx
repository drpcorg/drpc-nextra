# trace_callMany - Ethereum

## trace_callMany. Performs multiple call traces on top of the same block. i.e. transaction n will be executed on top of a pending block with all n-1 transactions applied (traced) first

import { EthereumMethod_trace_callMany } from "components/method-docs/ethereum/EthereumMethod_trace_callMany";

<EthereumMethod_trace_callMany />
