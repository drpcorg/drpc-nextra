# eth_createAccessList - Ethereum

## eth_createAccessList. Creates an EIP2930 access list from a Transaction object, returning addresses, storage keys used, and the estimated gas consumption with the access list included

import { EthereumMethod_createAccessList } from "components/method-docs/ethereum/EthereumMethod_createAccessList";

<EthereumMethod_createAccessList />
