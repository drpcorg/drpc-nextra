# eth_createAccessList - Ethereum

## eth_createAccessList. Returns list of addresses and storage keys that are read and written by the transaction (except the sender account and precompiles), plus the estimated gas consumed when the access list is added

import { EthereumMethod_createAccessList } from "components/method-docs/ethereum/EthereumMethod_createAccessList";

<EthereumMethod_createAccessList />
