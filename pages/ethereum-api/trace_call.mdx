# trace_call - Ethereum

## trace_call. Executes a new message call immediately without creating a transaction on the blockchain, and returns detailed trace information about the execution

import { EthereumMethod_trace_call } from "components/method-docs/ethereum/EthereumMethod_trace_call";

<EthereumMethod_trace_call />
