# eth_getCode - Sonic

## eth_getCode. Fetches the bytecode stored at a specific address for a given block, typically used to retrieve the deployed contract's code.

import { SonicMethod_getCode } from "components/method-docs/sonic/SonicMethod_getCode";

<SonicMethod_getCode />
