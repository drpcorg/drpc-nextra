# trace_call  - Celo

## trace_call. Simulates the execution of a transaction without broadcasting it to the network

import { CeloMethod_trace_call } from "components/method-docs/celo/CeloMethod_trace_call";

<CeloMethod_trace_call />
