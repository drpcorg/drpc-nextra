# net_peerCount - Viction

## net_peerCount. Returns the count of peers connected to the node, offering a snapshot of the network's connectivity.

import { VictionMethod_net_peerCount } from "components/method-docs/viction/VictionMethod_net_peerCount";

<VictionMethod_net_peerCount />
