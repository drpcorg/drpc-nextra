# eth_getProof - Viction

## eth_getProof. Generates a cryptographic proof for an account's state and specified storage keys at a given block, enabling trustless verification of on-chain data.

import { VictionMethod_getProof } from "components/method-docs/viction/VictionMethod_getProof";

<VictionMethod_getProof />
