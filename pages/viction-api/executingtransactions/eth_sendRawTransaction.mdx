# eth_sendRawTransaction - Viction

## eth_sendRawTransaction. Submits a signed, serialized transaction (provided as a hexadecimal string) to the network

import { VictionMethod_sendRawTransaction } from "components/method-docs/viction/VictionMethod_sendRawTransaction";

<VictionMethod_sendRawTransaction />
