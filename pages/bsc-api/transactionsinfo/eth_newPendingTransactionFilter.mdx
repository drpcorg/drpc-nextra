# eth_newPendingTransactionFilter - BNB Smart Chain

## eth_newPendingTransactionFilter. Creates a filter to notify when new pending transactions are added to the transaction pool

import { BSCMethod_newPendingTransactionFilter } from "components/method-docs/bsc/BSCMethod_newPendingTransactionFilter";

<BSCMethod_newPendingTransactionFilter />
