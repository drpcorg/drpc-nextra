# eth_chainId - BNB Smart Chain

## eth_chainId. Returns the current network or chain ID, which is used to sign replay-protected transactions as introduced in EIP-155.

import { BSCMethod_chainId } from "components/method-docs/bsc/BSCMethod_chainId";

<BSCMethod_chainId/>
