# net_peerCount - BNB Smart Chain

## net_peerCount. Returns the number of peers currently connected to the node, allowing users to monitor the node's active network connections.

import { BSCMethod_net_peerCount } from "components/method-docs/bsc/BSCMethod_net_peerCount";

<BSCMethod_net_peerCount />
