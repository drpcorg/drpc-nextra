# eth_hashrate - BNB Smart Chain

## eth_hashrate. returns the current number of hashes per second being performed by the node, indicating its mining hashrate.

import { BSCMethod_hashrate } from "components/method-docs/bsc/BSCMethod_hashrate";

<BSCMethod_hashrate />
