# trace_call - BNB Smart Chain

## trace_call. Traces the execution of a specific contract call or transaction without broadcasting it to the network.

import { BSCMethod_trace_call } from "components/method-docs/bsc/BSCMethod_trace_call";

<BSCMethod_trace_call />
