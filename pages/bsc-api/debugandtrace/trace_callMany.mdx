# trace_callMany - BNB Smart Chain

## trace_callMany. Allows you to trace the execution of multiple contract calls or transactions in a single request.

import { BSCMethod_trace_callMany } from "components/method-docs/bsc/BSCMethod_trace_callMany";

<BSCMethod_trace_callMany />
