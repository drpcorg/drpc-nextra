# eth_newBlockFilter  - Optimism

## eth_newBlockFilter. Creates a filter for new block headers, enabling to monitor the new blocks on the Optimism blockchain

import { OptimismMethod_newBlockFilter } from "components/method-docs/optimism/OptimismMethod_newBlockFilter";

<OptimismMethod_newBlockFilter />
