# trace_call  - Optimism

## trace_call. Simulates the execution of a transaction without broadcasting it to the network

import { OptimismMethod_trace_call } from "components/method-docs/optimism/OptimismMethod_trace_call";

<OptimismMethod_trace_call />
