# getbestblockhash - Bitcoin

## getbestblockhash. Retrieves the hash identifier of the most recent (latest) block in the Bitcoin blockchain.

import { BitcoinMethod_getbestblockhash} from "components/method-docs/bitcoin/BitcoinMethod_getbestblockhash";

<BitcoinMethod_getbestblockhash />
