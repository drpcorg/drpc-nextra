import { GetStarted } from "../../components/GenericMethod/getStarted/GetStarted";
import { Space } from "@mantine/core";
import { Meta } from "../../components/Meta";

## Smart Contract Execution

Facilitates the execution of smart contract methods and handles JSON-RPC requests.

[runGetMethod](/ton-api/smartcontractexecution/runGetMethod): Runs a specific smart contract method and returns the result. \
[messages](/ton-api/smartcontractexecution/messages): Retrieves a list of messages. \
[jsonRPC](/ton-api/smartcontractexecution/jsonRPC): Executes general JSON-RPC operations on the network.

This group enables interaction with smart contracts, making it possible to execute and manage smart contract methods while streamlining JSON-RPC-based interactions with the blockchain.

<Space h={16} />
<GetStarted />
