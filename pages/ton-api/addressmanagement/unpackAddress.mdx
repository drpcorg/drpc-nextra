# unpackAddress - Ton

## unpackAddress. Decodes a previously packed address back into its original format.

import { TonMethod_unpackAddress } from "components/method-docs/ton/TonMethod_unpackAddress";

<TonMethod_unpackAddress />
